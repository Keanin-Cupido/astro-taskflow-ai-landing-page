---
import Container from './Container.astro';
import Button from './Button.astro';
---

<section class="py-24 bg-white dark:bg-secondary-900">
  <Container>
    <div class="max-w-4xl mx-auto text-center animate-on-scroll">
      <h2 class="text-3xl sm:text-4xl font-bold text-secondary-900 dark:text-white mb-4">
        Ready to Transform Your Workflow?
      </h2>
      <p class="text-lg text-secondary-600 dark:text-secondary-400 mb-8">
        Join thousands of teams already using TaskFlow AI to streamline their projects and boost productivity.
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <Button href="#" size="lg">Start Free Trial</Button>
        <Button href="#" variant="outline" size="lg">Schedule Demo</Button>
      </div>
      <p class="mt-6 text-sm text-secondary-600 dark:text-secondary-400">
        No credit card required • 14-day free trial • Cancel anytime
      </p>
    </div>
  </Container>
</section>